import XCTest
@testable import arona_script_recognition

final class arona_script_recognitionTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(arona_script_recognition().text, "Hello, World!")
    }
}
